class Rectangle {
    constructor(w, h) {
        this.width = w;
        this.height = h;
    }
}

const rectangle = new Rectangle(3, 3);
console.log(`Instance width: ${rectangle.width} - height: ${rectangle.height}`);
