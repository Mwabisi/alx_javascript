#!/usr/bin/node
class Rectangle {
    constructor(w , h) {
        this.width = width;
        this.height = height;
    }
}

const rectangle = new Rectangle(3, 3);
console.log('${rectangle.width} - height: ${rectangle.height}');